{
  "name": "alkebulan-storefront",
  "private": true,
  "engines": { "node": ">=24" },
  "scripts": {
    "check": "node scripts/check.cjs",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "node --test tests/browser/*.test.cjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "playwright": "1.63.0"
  }
}
